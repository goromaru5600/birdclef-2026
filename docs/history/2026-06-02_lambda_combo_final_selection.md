# 2026-06-02 作業履歴: lambda実験・合わせ技・最終2枠選択

## 開始時の状況
- ベース: `eos9-adopt` public LB **0.950**（bronze line）
- 前日（6/1）の4本実験（genus020/010, xSED585, delta015）が全て **0.950**
- 残り手段を模索、締切 2026-06-03

---

## 1. lambda_prior 実験（2本）

EoS.9 Cell 23 の `apply_prior(meta_te, lambda_prior=0.75)` を変更。

| slug | 変更 | スコア |
|------|------|--------|
| `eos9-lambda-prior-070` | 0.75→**0.70** | 0.950 |
| `eos9-lambda-prior-080` | 0.75→**0.80** | **0.948**（悪化） |

**教訓：prior を強める（0.80）方向は有害。0.75 が既に最適点。**

---

## 2. 合わせ技 × 2本

単独変更が全て中立だったため、複数パラメータを同時変更。

| slug | 変更内容 | スコア |
|------|----------|--------|
| `eos9-xsed585-genus010` | xSED[0.585] + genus_α 0.10 | 0.950 |
| `eos9-xsed585-genus020` | xSED[0.585] + genus_α 0.20 | 0.950 |

---

## 3. 最終公開ノートブック・ディスカッション調査

締切前の最終スキャン。

| 確認項目 | 結果 |
|---|---|
| 公開NB スコア降順 | EoS.9 (0.950) が引き続き公開上限 |
| pilkwang PCEN sidecar | 403 のまま（private 依存、再現不可） |
| AnthonyTherrien ensemble | EoS.9 同等パイプライン、0.950 |
| 最新ディスカッション | 「huge shake?」「timeout」など技術的新情報なし |

**結論：0.950 を超える公開情報・手法は存在しない。**

---

## 4. 最終 2 枠選択

### 候補比較

| 提出 | 変更 | スコア |
|---|---|---|
| `eos9-adopt` | なし（ベースライン） | 0.950 |
| `eos9-tax-genus010` | genus_α 0.15→0.10 | 0.950 |
| `eos9-tax-genus020` | genus_α 0.15→0.20 | 0.950 |
| `eos9-xsed-0585` | xSED [0.60→0.585] | 0.950 |
| `eos9-delta-smooth-015` | delta_smooth 0.20→0.15 | 0.950 |
| `eos9-lambda-prior-070` | lambda_prior 0.75→0.70 | 0.950 |
| `eos9-xsed585-genus010` | xSED[0.585] + genus_α 0.10 | 0.950 |
| `eos9-xsed585-genus020` | xSED[0.585] + genus_α 0.20 | 0.950 |

### 選択結果

| 枠 | 選択 | 根拠 |
|---|---|---|
| 枠 1 | `eos9-adopt` | ベースライン確実 |
| 枠 2 | `eos9-xsed585-genus010` | 2パラメータ変更で最も eos9-adopt と異なる予測分布。両変更とも「平滑化を減らす」方向で一貫。 |

Kaggle の auto-selection が 2/2 になっていることを確認。

---

## 実験全履歴サマリー（コンペ全体）

| 実験 | スコア |
|---|---|
| eos9-adopt（ベース） | **0.950** |
| v3 blend（B0 CNN w=0.05） | 0.949 |
| genus_α 0.20 | 0.950 |
| genus_α 0.10 | 0.950 |
| xSED [0.585, 0.415] | 0.950 |
| delta_smooth 0.15 | 0.950 |
| lambda_prior 0.70 | 0.950 |
| lambda_prior **0.80** | **0.948** |
| xSED585 + genus010 | 0.950 |
| xSED585 + genus020 | 0.950 |

**EoS.9 は全軸で局所最適。0.950 が実質的な公開上限。**

---

## 教訓

1. **prior を強める方向（lambda 0.80）は有害** — 0.75 が既に最適
2. **合わせ技も突破口にならない** — 単独中立の組み合わせは期待値変わらず
3. **EoS.9 のポストプロセスは既に高度に最適化済み** — パラメータ探索の余地がほぼない
4. **0.950 密集帯の突破には独立した多様なモデルが必須** — 公開期間では再現不能
